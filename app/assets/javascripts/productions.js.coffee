# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/
#$ ->
  #$('[data-changetype]').click ->
  #  $.get($(this).data("changetype"),
  #    protype:$(this).data("protype"))
  #return
 # $('[data-qita]').click ->
  #  $("#bai").click();
    false
    #$.get($(this).data("qita"),
    #  protype:$(this).data("protype"),
    #way_display:$(this).data("way_display"),"html")
  #$("li").filter('.item').click ->
   # alert("ok")
  #$(".item88>a").bind('click', ->
  #    alert("ok")
  #    false)
#return
$(document).ready ->
  $("#Z").click ->
    $("#bai").click()
    return

  return
